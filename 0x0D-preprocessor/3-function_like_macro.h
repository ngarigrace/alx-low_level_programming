#ifndef MACROLIKE_HEADER
#define MACROLIKE_HEADER
/**
  *File name: 3-function_like_macro.h
  *@x: input integer
  *Description:  computes the absolute value of a number x
  */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
